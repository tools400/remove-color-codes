# Build Configuration for Remove Color Codes Project
# This file contains properties used by the Ant build script (build.xml)

# Project name
project.name=RemoveColorCodes

# Project version - Update this for a new release
project.version=1.2.1

# Project version - Don't touch
# Maven format (used in pom.xml): X.Y.Z-SNAPSHOT
project.version.maven=${project.version}-SNAPSHOT

# OSGi format (used in feature.xml and MANIFEST.MF): X.Y.Z.qualifier
project.version.osgi=${project.version}.qualifier

# Path to Maven executable
# Can be just 'mvn' if Maven is in your PATH, or full path like C:/apache-maven-3.9.5/bin/mvn.cmd
maven.executable=c:/Users/traddatz/AppData/Local/Maven-3.9.6/bin/mvn.cmd
